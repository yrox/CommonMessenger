class Message {
    constructor(type, accountId, text, metacontactName, metacontactId, id) {
        this.type = type;
        this.accountId = accountId;
        this.text = text;
        this.metacontactName = metacontactName;
        this.metacontactId = metacontactId;
        this.id = id;        
    }
}