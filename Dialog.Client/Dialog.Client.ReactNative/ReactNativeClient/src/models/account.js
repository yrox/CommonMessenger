class Account {
    constructor(type, accountId, login, pass, userName, userId, id) {
        this.type = type;
        this.accountId = accountId;
        this.login = login;
        this.pass = pass;
        this.userName = userName;
        this.userId = userId;
        this.id = id;        
    }
}